import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: "#006769",
        light:"#9DDE8B",
        dark:"006769"
      },
    },
  });
export {theme};